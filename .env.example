# docker compose reads .env from repo root automatically. Copy to .env per-machine, don't commit.
STORAGE_PATH=./nexus-backend/storage
# Obligatorio, sin valor por defecto: el backend no arranca sin el.
# Genera uno propio por maquina (NO copies este archivo tal cual):
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET_KEY=
CORS_ORIGINS=http://localhost:5173
